{
  "name": "glow-studio-by-sofia",
  "version": "1.0.0",
  "private": true,
  "description": "Glow Studio by Sofia — Premium Beauty Salon Web Application",
  "scripts": {
    "build": "cd apps/web && pnpm run build",
    "start": "cd apps/web && pnpm run start",
    "dev:web": "cd apps/web && pnpm run dev",
    "dev:api": "cd apps/api && pnpm run dev",
    "dev:bot": "cd apps/bot && python main.py",
    "build:web": "cd apps/web && pnpm run build",
    "build:api": "cd apps/api && pnpm run build",
    "install:web": "cd apps/web && pnpm install",
    "install:api": "cd apps/api && pnpm install",
    "install:bot": "cd apps/bot && pip install -r requirements.txt",
    "install:all": "pnpm run install:web && pnpm run install:api",
    "db:generate": "npx prisma generate",
    "db:push": "npx prisma db push",
    "db:seed": "npx tsx prisma/seed.ts",
    "db:studio": "npx prisma studio",
    "db:migrate": "npx prisma migrate dev",
    "db:reset": "npx prisma migrate reset",
    "postinstall": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "next": "16.2.10",
    "pg": "^8.22.0",
    "prisma": "^6.9.0",
    "react": "19.2.8",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
